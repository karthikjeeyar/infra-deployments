kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: keycloak-auth
spec: 
  path: /
  to:
    kind: Service
    name: env-boot-keycloak
    weight: 100
  port:
    targetPort: keycloak
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None